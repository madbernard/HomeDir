# comments start with a hash
[user]
	name = Mad Bernard
	email = github@z-amber.com

[core]
	autocrlf = input
	safecrlf = false
	editor = code --wait
	excludesfile = ~/.gitignore_global

[alias]
  hist = log --pretty=format:'%h %ad | %s%d [%an]' --graph --date=short

[push]
	default = simple
